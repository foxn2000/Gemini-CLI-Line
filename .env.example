GEMINI_API_KEY = your_gemini_api_key_here
LINE_CHANNEL_ACCESS_TOKEN = your_line_channel_access_token_here
LINE_CHANNEL_SECRET = your_line_channel_secret_here

# Geminiに渡すシステムプロンプト（オプション）
# 指定しない場合は、デフォルトで「応答の語尾は必ず「〜のだ」とすること。」が使用されます。
#SYSTEM_PROMPT="あなたのAIアシスタントです。"

# 会話履歴を遡る時間（時間単位、オプション）
# 指定しない場合は、デフォルトで1時間が使用されます。
#CONTEXT_TIME_LIMIT_HOURS=1

# (任意) デフォルトの作業ディレクトリを設定します。
# 未設定の場合は、main.py があるディレクトリが使用されます。
#DEFAULT_WORKDIR="/path/to/your/default/directory"
